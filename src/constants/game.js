const GAME_WIDTH = 1080;
const GAME_HEIGHT = 720;
const MOVE_KEYS = {
  37: true,
  38: true,
  39: true,
  40: true,
}

const gameConstants = {
  GAME_WIDTH,
  GAME_HEIGHT,
  MOVE_KEYS,
};

export default gameConstants;